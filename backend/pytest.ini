[pytest]
# Run from the backend/ directory: `python -m pytest`
# pythonpath = . lets tests import the app packages (services, routers, ...)
pythonpath = .
asyncio_mode = auto
norecursedirs = venv .venv __pycache__ alembic node_modules *.egg-info
addopts = -ra
